VITE_API_BASE_URL=http://localhost:8092/api
VITE_AUTH_BASE_URL=http://localhost:8092

# Backend JWT signing secret (HS256) used by tests to mint a token.
# This MUST match the jwt.secret in your backend application.yml
TEST_JWT_SECRET=

# Test user identity for local automated testing
TEST_USER_EMAIL=
TEST_USER_ID=
TEST_USER_NAME=
TEST_USER_ROLE=USER
